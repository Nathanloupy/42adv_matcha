// ── Age ──
export const AGE_MIN = 3;
export const AGE_MAX = 21;

// ── Fame ──
export const FAME_MIN = -1000;
export const FAME_MAX = 1000;

// ── Distance ──
export const DEFAULT_MAX_DISTANCE = 100;

// ── Tags ──
export const MAX_TAGS = 10;
export const DEFAULT_MIN_TAGS = 0;
